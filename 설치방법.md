# 📱 자동 출석체크 앱 설치 방법 (아주 자세히)

처음 하시는 분도 따라 할 수 있게 하나하나 적었습니다.
**전체 10~15분** 정도 걸립니다.

---

# 1부. APK 파일 받기

APK = 안드로이드 앱 설치 파일입니다. (아이폰은 안 됩니다)

## ⭐ 가장 쉬운 방법 (휴대폰에서 바로)

### ① 휴대폰 브라우저에서 아래 주소를 엽니다
```
https://github.com/dongjjamong27-blip/music-app/releases/latest
```

### ② 아래로 내려서 **`auto-attendance.apk`** 를 누릅니다
- **로그인 필요 없습니다.**
- **압축 풀 필요 없습니다.**
- 바로 다운로드가 시작됩니다. (약 6MB)

### ③ "이 유형의 파일은 기기에 위험할 수 있습니다" 경고가 나오면
→ **`계속`** 또는 **`확인`** 을 누릅니다. (APK 를 받을 때 항상 나오는 안내입니다)

### ④ 다운로드가 끝나면 **`열기`** 를 누릅니다
→ 바로 2부(설치하기)로 넘어가세요.

> 다운로드 알림이 사라졌다면 **내 파일** 앱 → **Download** 폴더에서 찾으세요.

---

## 다른 방법 A. 컴퓨터로 받기

1. 컴퓨터 브라우저에서 `https://github.com/dongjjamong27-blip/music-app/releases/latest`
2. **`auto-attendance.apk`** 클릭 → 다운로드
3. 휴대폰으로 옮기기 (USB / 카카오톡 나에게 보내기 / 구글 드라이브)

---

## 다른 방법 B. Actions 에서 받기 (권장하지 않음)

Release 가 없을 때만 쓰세요. 로그인이 필요하고 zip 압축을 풀어야 해서 번거롭습니다.

1. `https://github.com/dongjjamong27-blip/music-app/actions` 접속 후 **로그인**
2. 맨 위 초록색 ✅ 줄 클릭
3. ⚠️ **여기서 `build` 를 누르면 안 됩니다.** `build` 는 작업 상세 화면이라 파일이 없습니다.
   → 제목(`APK 만들기 #3`)이 보이는 **요약 화면**에서 **맨 아래**로 내려야 **Artifacts** 칸이 나옵니다.
4. `auto-attendance-apk` 클릭 → zip 다운로드 → 압축 풀기 → `auto-attendance.apk`

---

# 2부. APK 설치하기

### ① 파일 관리자에서 `app-release.apk` 를 누릅니다
- 삼성폰: **내 파일** → `Download` 폴더

### ② "출처를 알 수 없는 앱" 경고가 나오면
```
보안상의 이유로 이 출처의 알 수 없는 앱은 설치할 수 없습니다
```
→ **`설정`** 버튼을 누르고 → **`이 출처 허용`** 을 켭니다 → 뒤로가기 → 다시 설치

### ③ "Play 프로텍트" 경고가 나오면
```
알 수 없는 앱입니다 / 개발자를 확인할 수 없습니다
```
→ **`무시하고 설치`** 또는 **`자세히` → `무시하고 설치`** 를 누릅니다
- 구글에 등록 안 된 앱이라서 나오는 정상적인 안내입니다.

### ④ `설치` → `완료`
- 앱 서랍에 **자동 출석체크** 아이콘(초록색 ✔)이 생깁니다.

---

# 3부. 권한 켜기 (⭐ 제일 중요한 부분)

앱을 열면 4단계 화면이 나옵니다. 위에서부터 차례대로 하세요.

## 1단계 — 접근성 권한 (이게 있어야 자동 클릭됩니다)

### ① 앱에서 **`설정 열기`** 버튼을 누릅니다
### ② **`설치된 앱`** 또는 **`다운로드된 앱`** 항목을 찾아 누릅니다
### ③ **`자동 출석체크 도우미`** 를 누르고 → **스위치를 켭니다**
### ④ "기기를 완전히 제어하도록 허용" 경고 → **`허용`**

### 🚨 이런 메시지가 나오면? (안드로이드 13 이상에서 자주 나옴)
```
제한된 설정
보안상의 이유로 이 설정을 사용할 수 없습니다
```
또는 스위치가 **회색으로 눌리지 않는 경우** — 아래대로 하세요:

1. 폰 **설정** → **애플리케이션**(또는 `앱`)
2. 목록에서 **`자동 출석체크`** 를 찾아 누릅니다
3. 오른쪽 위 **점 3개(⋮)** 를 누릅니다
4. **`제한된 설정 허용`** 을 누릅니다
5. 다시 접근성 설정으로 가면 스위치가 켜집니다 ✅

> 이건 인터넷에서 받은 앱을 보호하려는 안드로이드의 안전장치입니다.
> 한 번만 허용해 주면 됩니다.

### ⑤ 앱으로 돌아와서 1단계 옆에 **✅** 가 생겼는지 확인
- 안 생겼으면 앱을 한 번 껐다 켜 보세요.

---

## 2단계 — 출석체크 할 앱 등록

### ① 오른쪽 아래 **`+ 앱 추가`** 를 누릅니다
### ② 폰에 설치된 앱 목록이 나옵니다
- 위 검색칸에 앱 이름을 치면 빨리 찾을 수 있습니다.
- 원하는 앱을 누릅니다.

### ③ **누를 글자**를 적습니다 ⭐ 여기가 핵심
- 그 앱을 **직접 열어서 눈으로 본 버튼 글자 그대로** 적어야 합니다.
- 예: 화면에 `출석체크` 라고 써 있으면 → `출석체크`
- 여러 개 가능성이 있으면 쉼표로: `출석체크, 출석, 오늘출석`

> ❗ 글자가 조금이라도 다르면 못 찾습니다.
> 먼저 그 앱을 열어서 버튼 글자를 확인하고 오세요.

### ④ **`적은 순서대로 하나씩 누르기`** 체크박스
- **체크 안 함** (기본) → 적은 글자 중 화면에 보이는 것 **하나만** 누릅니다. 보통 이걸로 충분합니다.
- **체크 함** → 적은 **순서대로 전부** 누릅니다.
  - 예: `출석체크, 확인, 닫기` → 출석체크 누르고 → 확인 누르고 → 닫기 누름
  - 광고가 먼저 뜨는 앱이면: `닫기, 출석체크` 처럼 광고 닫기를 먼저 적으세요.

### ⑤ **`저장`** 을 누릅니다
- 여러 앱을 등록하려면 ②~⑤ 를 반복하세요.

---

## 3단계 — 실행 시간 정하기

### ① **`시간 바꾸기`** → 매일 실행할 시간 선택
- 💡 **폰을 쓰고 있을 만한 시간**으로 정하세요. (예: 아침 9시, 저녁 8시)
- 잠금화면(비밀번호/지문)은 앱이 풀 수 없습니다.

### ② **`자동 실행`** 스위치를 **켭니다**
- 이걸 안 켜면 자동으로 실행되지 않습니다.

---

## 4단계 — 배터리 절약에서 빼기 (꼭 하세요)

이걸 안 하면 폰이 절전 중일 때 자동 실행이 **안 됩니다.**

### ① 앱에서 **`배터리 설정 열기`** 를 누릅니다
### ② **`제한 없음`** 또는 **`최적화 안 함`** 을 선택합니다

### 삼성폰은 하나 더 해 주세요
1. 폰 **설정** → **배터리** → **백그라운드 사용 제한**
2. **`절전이 적용되지 않는 앱`** 을 누릅니다
3. **`+`** → **`자동 출석체크`** 추가

### 샤오미 / 오포 / 원플러스 폰
- **설정** → **앱** → `자동 출석체크` → **자동 시작(자동 실행) 허용** 을 켜 주세요.

---

# 4부. 테스트하기

### ① 앱에서 **`지금 바로 한 번 실행해 보기`** 를 누릅니다
### ② **폰을 만지지 말고 그대로 두고 지켜보세요**
- 화면이 저절로 움직이면서 앱이 열리고 버튼이 눌립니다.
- 중간에 화면을 만지면 자동화가 방해받습니다.

### ③ 끝나면 앱으로 돌아와서 맨 아래 **실행 기록**을 확인합니다
- ✅ = 성공 (어떤 버튼을 눌렀는지 나옵니다)
- ❌ = 실패 (이유가 나옵니다)

---

# 5부. 안 될 때 해결 방법

| 증상 | 해결 방법 |
|---|---|
| ❌ **"글자를 못 찾았어요"** | 그 앱을 직접 열어 버튼 글자를 눈으로 확인 → `수정` 눌러서 똑같이 다시 적기 |
| ❌ **"접근성 서비스가 꺼져 있어요"** | 3부 1단계 다시 하기. 특히 **제한된 설정 허용** 확인 |
| ❌ **"앱을 열 수 없어요"** | 그 앱이 삭제됐는지 확인. 삭제 후 다시 등록 |
| **앱은 열리는데 안 눌림** | `수정` → **버튼 찾는 시간**을 `40` ~ `60` 초로 늘리기 (앱이 느리게 켜지는 경우) |
| **광고가 먼저 떠서 실패** | `순서대로 누르기` 켜고 → `닫기, 출석체크` 처럼 광고 닫기를 먼저 적기 |
| **정한 시간에 자동으로 안 됨** | ① 자동 실행 스위치 켜짐? ② 배터리 최적화 제외 했나? ③ 접근성 권한이 꺼지지 않았나? |
| **잠금화면에서 안 됨** | 비밀번호/지문 잠금은 풀 수 없습니다. 폰을 쓰는 시간대로 바꾸세요 |
| **접근성 스위치가 저절로 꺼짐** | 배터리 최적화 제외를 안 했을 때 생깁니다. 4단계를 다시 하세요 |

---

# 6부. 알아두면 좋은 것

- **같은 날 두 번은 안 합니다.** 이미 성공한 앱은 그날 다시 안 누릅니다.
  (테스트하려면 `지금 바로 한 번 실행해 보기` 를 누르세요 — 이건 기록을 지우고 다시 합니다)
- **일부만 성공해도 괜찮습니다.** 실패한 앱만 글자를 고쳐서 다시 시도하세요.
- **폰을 껐다 켜도** 설정은 그대로 유지되고 알람도 다시 걸립니다.
- **앱을 업데이트하려면** 앱 안의 **`5. 앱 업데이트` → `업데이트 확인`** 을 누르세요.
  브라우저를 거치지 않고 바로 새 버전을 받아 설치 화면까지 열어 줍니다. 설정과 기록은 안 지워집니다.
  - 처음 한 번은 **"이 출처 허용"** 을 물어봅니다. (이번엔 `자동 출석체크` 앱에 허용) 그 뒤로는 안 물어봅니다.

---

# 7부. 주의사항

- 본인 휴대폰에서 **본인 계정의 출석체크**를 편하게 하기 위한 개인용 도구입니다.
- 앱에 따라 자동화 사용을 약관으로 금지할 수 있으니, 사용 전에 해당 앱의 이용약관을 확인하세요.
- 접근성 권한은 화면 내용을 읽을 수 있는 강한 권한입니다. 이 앱은 **등록한 앱에서 등록한 글자를 찾아 누르는 용도로만** 사용합니다.
- 인터넷은 **`업데이트 확인` 을 눌렀을 때만** 씁니다. GitHub 에서 새 버전 파일을 받아오는 용도이며,
  화면 내용·설정·실행 기록 등 **어떤 정보도 밖으로 보내지 않습니다.** (보내는 코드 자체가 없습니다 — `Updater.kt` 에서 확인할 수 있습니다)
