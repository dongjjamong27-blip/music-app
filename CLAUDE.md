# 이 저장소에서 일하는 방법

이 앱의 주인은 **코드를 모르는 분**입니다.
휴대폰으로 블로그·SNS에 글을 올리는 데 쓰는 실제 도구이고, 화면의 모든 글은 한국어 존댓말입니다.

---

## 🚨 1순위 규칙 — 고쳤으면 반드시 `main` 까지 합칠 것

**푸시만 하면 아무 일도 일어나지 않습니다.**

이 저장소는 Vercel에 붙어 있고, 운영에 나가는 브랜치는 **`main` 하나뿐**입니다.
작업 브랜치에 푸시하면 Vercel은 **연습용(Preview)** 만 만듭니다. 주인이 쓰는 진짜 앱 주소
(`music-app-murex-nine.vercel.app`)에는 **아무것도 반영되지 않습니다.**

실제로 이 실수가 있었습니다. 새 기능을 3개나 올려놓고 "배포했습니다" 라고 말했지만,
주인은 앱에서 그 기능을 찾지 못했습니다. 브랜치에만 있었기 때문입니다.

### 그래서 작업은 여기서 끝나는 게 아닙니다

```
❌ 코드 수정 → 커밋 → 푸시 → "다 됐습니다"
✅ 코드 수정 → 커밋 → 푸시 → PR 생성 → main 에 병합 → "다 됐습니다"
```

**푸시한 뒤 PR을 만들고 병합까지 마친 다음에** 완료라고 말하세요.
주인은 이미 "하라고 하는 것도 전부 자동으로" 라고 허락했습니다. 병합 전에 다시 묻지 마세요.

병합 후에는 **1~2분 기다려야 앱에 나온다**는 것도 함께 알려주세요.

---

## 배포에 대해 알아둘 것

- **운영 브랜치는 `main`** 입니다. 다른 브랜치는 전부 Preview 입니다.
- Vercel 서버는 **파일을 쓸 수 없습니다** (읽기 전용). 저장이 필요한 기능은
  `localStorage`(`src/lib/localStore.ts`)를 쓰거나, 안 되면 화면에 이유를 분명히 보여주세요.
- 무료(Hobby) 요금제입니다. 함수 시간 **60초 이하**, 예약 실행은 **하루 1번**까지입니다.
- Vercel은 보안 취약점이 있는 Next.js 버전의 빌드를 **거부**합니다.
- `vercel.json` 의 `"framework": "nextjs"` 는 지우지 마세요. 지우면 모든 주소가 404 가 됩니다.

## 이 앱의 성격

- 네이버 블로그·헬로톡은 **일부러 반자동**입니다. 복사해주고 앱을 열어줄 뿐입니다.
  매크로·자동 로그인 같은 우회 방법은 계정 정지 위험 때문에 **쓰지 않습니다.**
- 유튜브·인스타는 공식 통로가 있어 완전 자동입니다. 다만 지금은 저장소가 없어 못 씁니다.
- 화면 문구는 **초등학생도 알아들을 수 있게** 씁니다. 전문용어를 쓰면 괄호로 풀어주세요.
- 코드 주석도 한국어로, **왜 이렇게 했는지**를 남깁니다.

## 확인하고 넘어가기

```bash
npx tsc --noEmit
npm run build
```

둘 다 통과한 뒤에 푸시하세요.
